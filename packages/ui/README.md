## @repo/ui

Shared UI component library intended to be consumed inside this monorepo via npm workspaces.

### Local development

From repo root:

```bash
npm install
npm --workspace @repo/ui run build
```

